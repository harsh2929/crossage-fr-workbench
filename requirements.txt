pillow==12.3.0
certifi>=2025.0.0
pillow-heif>=0.16
rawpy>=0.27
numpy>=1.26
opencv-python>=4.8
imageio-ffmpeg>=0.5
scikit-learn>=1.4
mcp[cli]>=1.27
# Security-fixed transitive pins. Keep these explicit so a universal lock
# refresh cannot select vulnerable versions still allowed by upstream ranges.
onnx==1.22.0
pydantic-settings==2.14.2
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
# C2PA 2.4 Content Credentials for signed generative edits and rendered exports.
# The build preflight also pins the bundled native c2pa-rs SDK revision.
c2pa-python==0.36.0
# Encrypted workspace catalog. The wheel statically bundles SQLCipher 4.x on
# supported CPython/macOS/Windows release targets.
sqlcipher3==0.6.2

# Read-only Apple Photos catalog access. The platform marker keeps Windows and
# Linux installs independent of osxphotos and its macOS framework dependencies.
osxphotos==0.76.1; sys_platform == "darwin"
Mako==1.3.12; sys_platform == "darwin"

# Offline reverse geocoding (APL-PLACE-01): bundled GeoNames gazetteer, no runtime network.
reverse_geocode>=1.6
# On-device OCR (PHOTO-01): Python 3.13-compatible RapidOCR with pinned PP-OCRv6 ONNX models.
rapidocr==3.9.1
# On-device tokenizer for SigLIP2 semantic search (APL-SEARCH-SEMANTIC): loads tokenizer.json from disk, offline at inference.
tokenizers>=0.20
