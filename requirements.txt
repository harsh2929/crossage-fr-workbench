pillow>=10.0
certifi>=2025.0.0
pillow-heif>=0.16
rawpy>=0.27
numpy>=1.26
opencv-python-headless>=4.8
imageio-ffmpeg>=0.5
scikit-learn>=1.4
mcp[cli]>=1.27

# Offline reverse geocoding (APL-PLACE-01): bundled GeoNames gazetteer, no runtime network.
reverse_geocode>=1.6
# On-device OCR (APL-INTEL-01/META-07): RapidOCR PP-OCRv5 ONNX over onnxruntime, offline.
rapidocr-onnxruntime>=1.4
# On-device tokenizer for SigLIP2 semantic search (APL-SEARCH-SEMANTIC): loads tokenizer.json from disk, offline at inference.
tokenizers>=0.20
